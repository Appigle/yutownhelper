<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activity.TestMenuActivity">

    <Button
        android:id="@+id/btn_test"
        style="@style/btn_normal_style"
        android:text="长按上下文菜单按钮"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@+id/btn_context_menu"
        android:layout_alignStart="@+id/btn_context_menu"
        android:layout_marginTop="182dp" />

    <Button
        android:id="@+id/btn_context_menu"
        style="@style/btn_normal_style"
        android:text="点击上下文菜单按钮"
        android:layout_below="@+id/btn_test"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="48dp" />
</RelativeLayout>
